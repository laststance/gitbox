# 機能仕様：Vibe Rush - GitHub Repository 管理アプリ

**Feature Branch**: `001-github-repo-manager`
**作成日**: 2025-11-15
**ステータス**: Draft
**入力**: Vibe RushというGithub Repositoryの管理Webアプリを作成します。仕様が PRD.md ファイルに記載されているので全て読んで理解してください。

## ユーザーシナリオ & テスト *(必須)*

### ユーザーストーリー 1 - GitHub ログイン (優先度: P1)

開発者として、自分の GitHub アカウントでログインして、管理している Repository にアクセスしたい。

**優先度の理由**: 認証は全ての機能の前提条件であり、セキュアなアクセスを保証するために最優先。

**独立したテスト**: GitHub OAuth フローを実行し、認証後に Boards 画面に遷移することで完全にテスト可能。ユーザーに認証された状態でアプリケーションへのアクセスを提供する価値を実現。

**受け入れシナリオ**:

1. **Given** 未認証ユーザーがトップページ（Landing Page）にアクセスした、**When** "Sign in with GitHub" ボタンをクリックする、**Then** GitHub OAuth フローが開始される
2. **Given** GitHub OAuth フローで認証が成功した、**When** リダイレクトされる、**Then** Boards 画面（Home）に遷移し、ユーザーの Repository 一覧が表示可能な状態になる
3. **Given** 認証に失敗した、**When** エラーが発生する、**Then** 分かりやすいエラーメッセージが表示され、再度ログインを試行できる

**ブラウザ検証要件**:
- [x] 視覚的検証が必要（スクリーンショット必須）
- [x] インタラクション テストが必要（Playwright 自動化）
- [x] アクセシビリティ準拠が必要（WCAG AA）

---

### ユーザーストーリー 2 - Repository 検索と追加 (優先度: P1)

開発者として、管理したい GitHub Repository を素早く検索して、Kanban ボードに追加したい。

**優先度の理由**: Repository の追加は、アプリケーションの主要な価値提供であり、MVP の中核機能。これがないと他の機能が意味を持たない。

**独立したテスト**: Combobox で Repository を検索し、選択して追加することで完全にテスト可能。追加された Repository がボードに表示され、"Quick note" にフォーカスが移動することで価値を実現。

**受け入れシナリオ**:

1. **Given** 認証済みユーザーが Boards 画面にいる、**When** "Add Repositories" ボタンをクリックする、**Then** Repository 検索用の Combobox が表示される
2. **Given** Combobox が表示されている、**When** owner/repo 名、topics、または visibility で検索する、**Then** 条件に一致する Repository の候補が表示される
3. **Given** 100+ repositories を持つユーザーが検索する、**When** 検索キーワードを入力する、**Then** 1秒以内に候補が表示される（遅延なし）
4. **Given** Repository 候補が表示されている、**When** 複数の Repository を選択して "Add" をクリックする、**Then** 選択した Repository が全て Board に追加される
5. **Given** 既に追加済みの Repository を選択しようとする、**When** 追加を試みる、**Then** 重複検知され、エラーメッセージが表示される
6. **Given** Repository が追加された、**When** 追加処理が完了する、**Then** 自動的に "Quick note" 入力欄にフォーカスが移動する

**ブラウザ検証要件**:
- [x] 視覚的検証が必要（スクリーンショット必須）
- [x] インタラクション テストが必要（Playwright 自動化）
- [x] アクセシビリティ準拠が必要（WCAG AA）

---

### ユーザーストーリー 3 - Kanban ボードで Repository 管理 (優先度: P1)

開発者として、Kanban ボード形式で Repository の状態（Status）と優先度を視覚的に管理したい。

**優先度の理由**: Kanban ボードはアプリケーションの中核 UI であり、Repository の状態管理を実現する主要機能。MVP として必須。

**独立したテスト**: Repository カードをドラッグ＆ドロップして列間移動（Status 変更）と列内順序変更（優先度変更）を行い、変更が即座に反映されることで完全にテスト可能。

**受け入れシナリオ**:

1. **Given** Repository カードが Board に追加されている、**When** カードを別の列（Status）にドラッグ＆ドロップする、**Then** Repository の Status が更新され、新しい列に表示される
2. **Given** Repository カードが列内にある、**When** 同じ列内でカードを上下にドラッグする、**Then** カードの優先度（order）が変更され、新しい位置に表示される
3. **Given** ドラッグ＆ドロップ操作を実行する、**When** ドロップする、**Then** 100ms 以内に変更が反映される（即座の応答）
4. **Given** 列に WIP limit が設定されている、**When** limit を超えるカードを追加しようとする、**Then** 警告が表示される
5. **Given** カードの移動を実行した、**When** "Z" キーを押す、**Then** 最後の1手が Undo される
6. **Given** Repository カードが表示されている、**When** カード右上の ⋯ メニューをクリックする、**Then** Overflow menu が表示され、"Open on GitHub"、"Edit Project Info" などのアクションが選択できる

**ブラウザ検証要件**:
- [x] 視覚的検証が必要（スクリーンショット必須）
- [x] インタラクション テストが必要（Playwright 自動化）
- [x] アクセシビリティ準拠が必要（WCAG AA）

---

### ユーザーストーリー 4 - Project Info でメモとリンクを管理 (優先度: P1)

開発者として、各 Repository に関する一言メモ、本番 URL、ダッシュボード URL などの情報を記録・参照したい。

**優先度の理由**: Repository に関するコンテキスト情報を記録することで、開発者の認知負荷を軽減し、素早い参照を可能にする。MVP として必須。

**独立したテスト**: Repository カードの ⋯ メニューから "Edit Project Info" を選択し、モーダルで Quick note と Links を編集できることで完全にテスト可能。

**受け入れシナリオ**:

1. **Given** Repository カードが存在する、**When** カード右上の ⋯ メニューから "Edit Project Info" を選択する、**Then** Project Info モーダルが表示される
2. **Given** Project Info モーダルが表示されている、**When** Quick note セクションに1〜3行のメモを入力する、**Then** メモが保存される
3. **Given** Links セクションにいる、**When** Production URL を複数追加する、**Then** 全ての URL が保存され、リスト表示される
4. **Given** Links セクションにいる、**When** Tracking services（GA/GTM/Plausible など）のダッシュボード URL を追加する、**Then** URL が保存され、クリックで新しいタブで開ける
5. **Given** Links セクションにいる、**When** Supabase Dashboard のリンクを追加する、**Then** プロジェクト/Branch/Pooler などへの参照リンクが保存される
6. **Given** Project Info を編集した、**When** "Save" ボタンをクリックする、**Then** 変更が保存され、モーダルが閉じる

**ブラウザ検証要件**:
- [x] 視覚的検証が必要（スクリーンショット必須）
- [x] インタラクション テストが必要（Playwright 自動化）
- [x] アクセシビリティ準拠が必要（WCAG AA）

---

### ユーザーストーリー 5 - 機密情報を安全に管理 (優先度: P2)

開発者として、OAuth Secret や API Key などの機密情報を安全に保存・管理したい。

**優先度の理由**: セキュリティは重要だが、基本的な Repository 管理機能（P1）が動作すれば MVP として価値を提供できる。セキュリティ強化フェーズで実装。

**独立したテスト**: Project Info モーダルの Credentials セクションで、3つの管理パターン（参照リンク、暗号化保存、外部管理）を使用して機密情報を追加・管理できることで完全にテスト可能。

**受け入れシナリオ**:

1. **Given** Project Info モーダルの Credentials セクションにいる、**When** 参照リンク型（Type: Reference）の認証情報を追加する、**Then** ダッシュボード URL のみが保存され、実際の値はサービス側で管理される
2. **Given** Credentials セクションにいる、**When** 暗号化保存型（Type: Encrypted）の認証情報を追加する、**Then** 値が AES-256-GCM で暗号化され、マスク表示（例: `github_*****xyz789`）される
3. **Given** 暗号化された認証情報が表示されている、**When** "Reveal" ボタンをクリックする、**Then** 暗号化された値が30秒間表示され、その後自動的にマスク表示に戻る
4. **Given** 暗号化された認証情報にアクセスした、**When** "Reveal" または "Copy" を実行する、**Then** アクセスが監査ログに記録される（timestamp, userId, action, resourceId を含む）
5. **Given** Credentials セクションにいる、**When** 外部管理型（Type: External）の認証情報を追加する、**Then** 1Password/Bitwarden などでの保管場所（例: "Team Vault > Production"）が記録される

**ブラウザ検証要件**:
- [x] 視覚的検証が必要（スクリーンショット必須）
- [x] インタラクション テストが必要（Playwright 自動化）
- [x] アクセシビリティ準拠が必要（WCAG AA）

---

### ユーザーストーリー 6 - Maintenance Mode で完了プロジェクトを整理 (優先度: P2)

開発者として、完了・保守中のプロジェクトを別の場所に整理して、アクティブなプロジェクトに集中したい。

**優先度の理由**: プロジェクト整理は便利だが、基本的な Kanban 管理（P1）が動作すれば価値を提供できる。完全版フェーズで実装。

**独立したテスト**: Sidebar から Maintenance Mode に遷移し、完了プロジェクトを Grid/List 表示で参照・復元できることで完全にテスト可能。

**受け入れシナリオ**:

1. **Given** Repository カードの ⋯ メニューにいる、**When** "Move to Maintenance" を選択する、**Then** Repository が Maintenance Mode に移動され、Board から非表示になる
2. **Given** Sidebar にいる、**When** "Maintenance Mode" リンクをクリックする、**Then** Maintenance Mode 画面に遷移し、完了・保守中のプロジェクトが表示される
3. **Given** Maintenance Mode 画面にいる、**When** Grid/List ビュー切り替えボタンをクリックする、**Then** ワンクリックで即座にビューが切り替わる
4. **Given** Maintenance Mode の Repository カードにいる、**When** カードをクリックする、**Then** GitHub の Repository ページが新しいタブで開く
5. **Given** Maintenance Mode の Repository カード右上の ⋯ メニューにいる、**When** "Restore to Board" を選択する、**Then** Repository が Board に復元され、Maintenance Mode から削除される
6. **Given** Maintenance Mode にいる、**When** 検索欄でキーワード検索する、**Then** アーカイブされたプロジェクトが絞り込まれる

**ブラウザ検証要件**:
- [x] 視覚的検証が必要（スクリーンショット必須）
- [x] インタラクション テストが必要（Playwright 自動化）
- [x] アクセシビリティ準拠が必要（WCAG AA）

---

### ユーザーストーリー 7 - テーマをカスタマイズ (優先度: P3)

開発者として、12種類のテーマから好みの配色を選択して、快適に作業したい。

**優先度の理由**: テーマカスタマイズはUX向上に貢献するが、デフォルトテーマで基本機能は使用可能。完全版フェーズで実装。

**独立したテスト**: Settings 画面で 12 種類のテーマを選択し、プレビューして適用できることで完全にテスト可能。

**受け入れシナリオ**:

1. **Given** Sidebar の Settings にいる、**When** Theme セクションを表示する、**Then** 12 種類のテーマ（Light 6種、Dark 6種）が選択可能
2. **Given** Theme セクションにいる、**When** テーマを選択して "Preview" をクリックする、**Then** 選択したテーマのプレビューが表示される
3. **Given** テーマをプレビューしている、**When** "Apply" ボタンをクリックする、**Then** アプリケーション全体にテーマが適用される
4. **Given** 任意のテーマが適用されている、**When** 本文テキストのコントラスト比を測定する、**Then** 4.5:1 以上を満たす
5. **Given** 任意のテーマが適用されている、**When** UI コンポーネント/アイコンのコントラスト比を測定する、**Then** 3:1 以上を満たす

**ブラウザ検証要件**:
- [x] 視覚的検証が必要（スクリーンショット必須）
- [x] インタラクション テストが必要（Playwright 自動化）
- [x] アクセシビリティ準拠が必要（WCAG AA）

---

### ユーザーストーリー 8 - PWA としてインストール (優先度: P3)

開発者として、アプリを PWA としてインストールして、ネイティブアプリのように使用したい。

**優先度の理由**: PWA 対応はUX向上に貢献するが、Web ブラウザからでも全機能が使用可能。完全版フェーズで実装。

**独立したテスト**: ブラウザの「インストール」プロンプトからアプリをインストールし、スタンドアロンで起動できることで完全にテスト可能。

**受け入れシナリオ**:

1. **Given** ユーザーが対応ブラウザでアプリにアクセスした、**When** ブラウザが PWA をサポートしている、**Then** 「インストール」プロンプトが表示される
2. **Given** インストールプロンプトが表示されている、**When** 「インストール」をクリックする、**Then** アプリがデバイスにインストールされる
3. **Given** アプリがインストールされている、**When** アプリアイコンをクリックして起動する、**Then** スタンドアロンウィンドウで起動し、ブラウザ UI が表示されない
4. **Given** オフライン状態になった、**When** 基本機能（Board 表示、Quick note 編集など）を使用する、**Then** キャッシュされたデータで動作する

**ブラウザ検証要件**:
- [x] 視覚的検証が必要（スクリーンショット必須）
- [x] インタラクション テストが必要（Playwright 自動化）
- [x] アクセシビリティ準拠が必要（WCAG AA）

---

### エッジケース

- **大量の Repository**: 1000+ repositories を持つユーザーが検索した場合、候補表示が遅延しないか？ → 仮想スクロールやページネーションで対応
- **ネットワーク障害**: GitHub API が応答しない場合、どのようにエラーハンドリングするか？ → リトライ機構とユーザーフレンドリーなエラーメッセージ
- **暗号化鍵の紛失**: KMS の暗号化鍵が紛失・ローテーション失敗した場合、どう復旧するか？ → バックアップ鍵とリカバリー手順を定義
- **長時間のドラッグ操作**: ドラッグ中にネットワークが切断された場合、どう処理するか？ → 楽観的 UI 更新と失敗時のロールバック
- **同時編集**: 複数のタブ/デバイスで同じ Board を編集した場合、データ整合性をどう保つか？ → リアルタイム同期または最終書き込み優先とコンフリクト通知

## 要件 *(必須)*

### 機能要件

- **FR-001**: システムは GitHub OAuth を使用してユーザー認証を提供しなければならない
- **FR-002**: システムは Repository の検索と追加を可能にしなければならない（owner/repo, topics, visibility による検索）
- **FR-003**: システムは 100+ repositories でも遅延なく（1秒以内に）候補を表示しなければならない
- **FR-004**: システムは Kanban ボード形式で Repository を管理できなければならない
- **FR-005**: ユーザーはドラッグ＆ドロップで Status（列）と優先度（同列内順序）を変更できなければならない
- **FR-006**: ドラッグ＆ドロップ操作は 100ms 以内に応答しなければならない
- **FR-007**: Undo 操作は 200ms 以内に完了しなければならない
- **FR-008**: システムは各 Repository の Quick note（1〜3行）を保存できなければならない
- **FR-009**: システムは各 Repository に複数の Production URL、Tracking services URL、Supabase Dashboard URL を保存できなければならない
- **FR-010**: システムは3つの Credentials 管理パターン（参照リンク、暗号化保存、外部管理）をサポートしなければならない
- **FR-011**: 暗号化された認証情報は AES-256-GCM で保護されなければならない
- **FR-012**: 暗号化された認証情報はデフォルトでマスク表示（例: `github_*****xyz789`）されなければならない
- **FR-013**: "Reveal" で表示された認証情報は 30秒後に自動的にマスク表示に戻らなければならない
- **FR-014**: すべての機密情報アクセスは監査ログに記録されなければならない（timestamp, userId, action, resourceId を含む）
- **FR-015**: システムは完了プロジェクトを Maintenance Mode で管理できなければならない
- **FR-016**: Maintenance Mode は Grid/List ビュー切り替えをサポートしなければならない
- **FR-017**: Grid/List ビューの切り替えは即座に反映されなければならない（ワンクリックで即時）
- **FR-018**: システムは 12 種類のテーマ（Light 6種、Dark 6種）を提供しなければならない
- **FR-019**: すべてのテーマは WCAG AA 基準を満たさなければならない（本文テキスト 4.5:1、UI 要素 3:1 以上のコントラスト比）
- **FR-020**: システムはコントラスト比が基準を満たさない場合、ビルドを失敗させなければならない
- **FR-021**: システムは PWA として動作しなければならない（インストール可能、オフライン対応）
- **FR-022**: システムは英語と日本語の両方をサポートしなければならない
- **FR-023**: すべてのユーザー向けテキストは外部化され、翻訳可能でなければならない
- **FR-024**: 日付/時刻のフォーマットはユーザーのロケールに従わなければならない
- **FR-025**: キーボード操作のみで全ての主要機能にアクセスできなければならない
- **FR-026**: システムは Command Palette（⌘K）を提供しなければならない
- **FR-027**: システムはショートカットヘルプ（?）を提供しなければならない

### キーエンティティ

- **User**: GitHub アカウントで認証されたユーザー。認証情報、プロフィール情報を持つ。
- **Board**: Repository カードを整理する Kanban ボード。複数作成可能。名前、テーマ、WIP limits などの設定を持つ。
- **StatusList**: Board 内の列。Status（例: Suspend, Spec designing, Active, Completed）を表す。名前、色、WIP limit、order を持つ。
- **RepoCard**: GitHub Repository を表すカード。owner/name、Quick note、statusId、order、meta（stars, updatedAt, visibility, language, topics）を持つ。
- **ProjectInfo**: Repository の詳細情報。repoId、links（production, tracking, supabase）、credentials（配列）を持つ。
- **Credential**: 認証情報。3つのタイプ（reference, encrypted, external）を持つ。暗号化タイプは encrypted_value、encryption_key_id、masked_display を含む。
- **AuditLog**: 機密情報アクセスの記録。userId、action、resourceId、resourceType、timestamp、ipAddress、userAgent、success を持つ。
- **Maintenance**: 完了・保守中プロジェクトの管理。repoId、hidden フラグを持つ。
- **Theme**: UI テーマ設定。12種類（Light: Sunrise, Sandstone, Mint, Sky, Lavender, Rose / Dark: Midnight, Graphite, Forest, Ocean, Plum, Rust）。

## 成功基準 *(必須)*

### 測定可能な成果

- **SC-001**: ユーザーは GitHub アカウントで2分以内にログインして Boards 画面にアクセスできる
- **SC-002**: ユーザーは 100+ repositories を1秒以内に候補表示して検索・追加できる（遅延なし）
- **SC-003**: ドラッグ＆ドロップ操作が 100ms 以内に応答する（即座の応答）
- **SC-004**: Undo 操作が 200ms 以内に完了する
- **SC-005**: Grid/List ビューの切り替えが即座に反映される（ワンクリックで即時）
- **SC-006**: すべてのテーマで本文テキストが 4.5:1 以上、UI 要素が 3:1 以上のコントラスト比を達成する（自動テストで検証）
- **SC-007**: すべての機密情報アクセスが監査ログに記録される（記録漏れ0件）
- **SC-008**: PWA としてインストール可能で、オフライン時も基本機能（Board 表示、Quick note 編集）が動作する
- **SC-009**: 英語と日本語の両方で完全に動作する（全てのテキストが翻訳されている）
- **SC-010**: キーボード操作のみで全ての主要機能（ログイン、Repository 追加、ドラッグ＆ドロップ、Project Info 編集）にアクセスできる
- **SC-011**: 90%以上のユーザーが初回訪問時に主要タスク（Repository 追加と Kanban ボード管理）を完了できる
- **SC-012**: セキュリティテストで暗号化/復号プロセスと監査ログの完全性が100%検証される

## UI 仕様（テキストワイヤーフレーム）

**重要**: 全ての UI は以下のワイヤーフレームから Magic MCP を使用して生成する。

### Sidebar（全画面共通）

```
┌───────────────────────────────┐
│ Vibe Rush                      │
│ ───────────────────────────── │
│ Boards                         │
│  • All Boards                  │
│  • Favorites                   │
│ ───────────────────────────── │
│ Maintenance Mode               │  ← 完了・保守中プロジェクト
│ ───────────────────────────── │
│ Settings                       │
│ Shortcuts                      │
│ Profile & Sign out             │
└───────────────────────────────┘
```

### Board（Kanban with overflow menus）

```
┌──────────────────────────────────────────────────────────────────────┐
│ AI Experiments                                 ○ avatar   [?]       │
│ [ Add Repositories ] [ Filter ] [ Compact ] [ Refresh ] [ ⌘K ]     │
│                                                                      │
│ Suspend           Spec designing      Active             Completed    │
│ (WIP 3/4)         (WIP –)             (WIP 5/6)          (WIP –)      │
│ ┌───────────────────────────┐   ┌───────────────────────────┐         │
│ │ repo-ml-lab        [⋯]    │   │ ui-research        [⋯]    │         │
│ │ "On hold this week"       │   │ "Draft spec"              │         │
│ └───────────────────────────┘   └───────────────────────────┘         │
│ ↑ drag to reorder (priority)     ← drag across lists to change status │
└──────────────────────────────────────────────────────────────────────┘
```

### Project Info（Modal - 強化版）

```
┌───────────────────────────────────────────────────┐
│ Project Info – laststance/app-studio              │
│---------------------------------------------------│
│ Quick note                                        │
│ [ "Heads down on auth rollout…" ]                 │
│---------------------------------------------------│
│ Links                                             │
│ • Production URL(s):  https://app.example.com     │
│ • Tracking:           https://plausible.io/...    │
│ • Supabase:           https://supabase.com/...    │
│---------------------------------------------------│
│ Credentials                                       │
│                                                   │
│ 📎 Supabase API Key                              │
│    Type: Reference                               │
│    [Open Dashboard →]                            │
│                                                   │
│ 🔒 GitHub OAuth Secret                           │
│    Type: Encrypted (Stored securely)             │
│    Value: github_*****xyz789                     │
│    Created: 2025-01-15                          │
│    [Copy] [Reveal] ← 30秒表示後に自動マスク     │
│                                                   │
│ 🔑 Database Password                             │
│    Type: External (1Password)                    │
│    Location: Team Vault > Production             │
│    [Open 1Password →]                           │
│---------------------------------------------------│
│ [ Save ] [ Cancel ]                               │
└───────────────────────────────────────────────────┘
```

### Maintenance Mode（Explorer UI）

#### Grid View

```
┌───────────────────────────────────────────────────────────────┐
│ Maintenance Mode                              ○ avatar  [?]  │
│ [ Search archived... ] [ Grid ▾ ] [ Sort: Updated ▾ ]        │
│                                                               │
│ ┌───────────────┐  ┌───────────────┐  ┌───────────────┐       │
│ │ lib-utils  ⋯  │  │ ui-research ⋯ │  │ core-cli   ⋯  │  ...  │
│ │ "Shipped…"    │  │ "Docs only"   │  │ "Security"   │       │
│ │ Updated 2025-10-11  ⭐410         │  │ Updated 2025-07-01    │
│ └───────────────┘  └───────────────┘  └───────────────┘       │
│                                                               │
│ [Open on GitHub]  [Restore to Board]  [Hide]                  │
└───────────────────────────────────────────────────────────────┘
```

#### List View（同画面で切替）

```
Name              Note                 Updated        Stars   Actions
lib-utils         "Shipped…"           2025-10-11     410     [⋯]
ui-research       "Docs only"          2025-09-03     128     [⋯]
core-cli          "Security"           2025-07-01      87     [⋯]
```

### Settings

```
┌───────────────────────────────────────────────┐
│ Settings                                      │
│ Theme (12)                                    │
│  Light: Sunrise / Sandstone / Mint / Sky / ...│
│  Dark : Midnight / Graphite / Forest / ...    │
│  [ Preview ] [ Apply ]                        │
│-----------------------------------------------│
│ Typography                                    │
│  Base size: [16px ▾]  Step: [1px]            │
│  Scale preview: 12 13 14 15 16 18 20 22 24 28 │
│  (target: AA contrast on each theme)          │
└───────────────────────────────────────────────┘
```

### インタラクション仕様

#### Overflow "⋯" Menu（Board & Maintenance 共通）

各 Repository カード右上の三点リーダーは小さな一時的アクション群を開く Overflow menu。

**メニュー項目（共通）**:
```
Open on GitHub
Open Production URL
Open Tracking dashboard
Open Supabase dashboard
Edit Project Info…   // モーダル起動
Move to Maintenance  // Board のみ表示
Restore to Board     // Maintenance のみ表示
```

#### Shortcuts（統一）

- `.` … フォーカス中カードの Overflow menu
- `Enter` … 既定アクション（Board=Open card、Maintenance=Open on GitHub）
- `⌘K` … Command Palette
- `Z` … Undo last move（1手）
- `?` … Shortcuts help
